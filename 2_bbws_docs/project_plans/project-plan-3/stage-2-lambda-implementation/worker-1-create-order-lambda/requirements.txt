# Python dependencies for create_order Lambda function

# AWS SDK
boto3==1.34.0
botocore==1.34.0

# Data validation
pydantic==1.10.18

# Testing dependencies (dev only, not packaged in Lambda)
pytest==7.4.3
pytest-cov==4.1.0
pytest-mock==3.12.0
moto==4.2.9
